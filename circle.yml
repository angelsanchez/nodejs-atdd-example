machine:
    node:
        version: 4.2.2
    services:
        - mongodb
test:
    pre:
        - cp src/config_sample.json src/config.json
    override:
        - npm run test-unit
        - npm run test-integration
        - npm run test-coverage
